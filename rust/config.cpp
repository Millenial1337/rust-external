#include <stdio.h>
#include "Overlay/Menu/Settings.hpp"

//void SaveSettings() {
//	auto file = fopen(".gdh\\gdh-data.dat", "wb");
//	if (file) {
//		fwrite(&Settings::, sizeof(hacks), 1, file);
//		fclose(file);
//	}
//}